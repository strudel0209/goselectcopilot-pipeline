# NOT USABLE ON THIS NETWORK - use environment.yml (conda-forge) instead.
#
# files.pythonhosted.org is blocked by corporate egress policy, so plain
# `pip install -r requirements.txt` fails with:
#   SSLError(1, '[SSL: SSLV3_ALERT_HANDSHAKE_FAILURE] ...')
#
# Kept for portability to networks where PyPI is reachable, and as the
# dependency list to mirror into an Azure Artifacts feed.
#
# To install via a governed Azure Artifacts feed with a PyPI upstream
# (pkgs.dev.azure.com IS reachable), create the feed then:
#
#   pip install -r requirements.txt \
#     --index-url https://pkgs.dev.azure.com/<org>/<project>/_packaging/<feed>/pypi/simple/
#
# Authenticate with `az login` + artifacts-keyring, or a PAT supplied via
# ~/.config/pip/pip.conf. Never put a PAT on the command line.

azure-ai-documentintelligence==1.0.2
azure-identity>=1.19.0
python-dotenv>=1.0.1
pymupdf>=1.24.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.8.0
rapidfuzz>=3.9.0
ipykernel>=6.29.0
