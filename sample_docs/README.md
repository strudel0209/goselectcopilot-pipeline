Drop a real ABB mixed-content package here as `mixed_package.pdf`
(or point `SAMPLE_PDF` in `.env` at any path).

Ideal test file for the segmentation lab: one PDF containing
specification prose pages, tabular equipment/VFD schedule pages, and
at least one drawing sheet — including, if possible, a page that has
both a table and a diagram on it.

PDFs in this folder are gitignored.
